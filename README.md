# environment_narrator
A IoT project that "gives the environment a voice". Streams sensor data over MQTT to a Raspberry Pi, to query an LLM to generate a contextual interpretation of it's surroundings.
